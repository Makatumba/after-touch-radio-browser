{
  "name": "aftertouch-radio-browser",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "deploy": "npm run build && rimraf docs && mv dist docs"
  },
  "dependencies": {
    "axios": "^1.7.9"
  },
  "devDependencies": {
    "jsdom": "^25.0.1",
    "rimraf": "^6.1.3",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.8"
  }
}
